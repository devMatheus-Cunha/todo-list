import styled from 'styled-components';

export const Header = styled.header`
  padding: 2rem;
  font-size:1.5rem;

  span{
    font-size: 1.5rem;
    color: var(--gray);
  }
`;

export const Separation = styled.header`
 width:100%;
 height: 0.1px;
 background-color:var(--white-050);
 filter: brightness(20%);

 margin-top: 1rem;

`;